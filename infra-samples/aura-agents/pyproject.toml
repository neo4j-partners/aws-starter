[project]
name = "aura-agents"
version = "0.1.0"
description = "Python client for calling external Neo4j Aura Agents via REST API"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "pydantic>=2.12.5",
    "python-dotenv>=1.2.1",
]
