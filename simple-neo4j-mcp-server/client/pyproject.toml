[project]
name = "neo4j-mcp-client"
version = "0.1.0"
description = "Python client for testing the Neo4j MCP server"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "boto3>=1.35.0",
    "httpx>=0.27.0",
]
