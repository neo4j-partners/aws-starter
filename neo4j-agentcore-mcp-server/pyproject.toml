[project]
name = "neo4j-agentcore-mcp-server"
version = "0.1.0"
description = "Neo4j MCP server deployment for Amazon Bedrock AgentCore"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "boto3>=1.35.0",
    "httpx>=0.27.0",
    "botocore[crt]",
]
